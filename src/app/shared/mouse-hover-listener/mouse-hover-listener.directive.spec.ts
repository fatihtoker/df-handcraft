import { MouseHoverListenerDirective } from './mouse-hover-listener.directive';

describe('MouseHoverListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseHoverListenerDirective();
    expect(directive).toBeTruthy();
  });
});
